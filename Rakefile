require 'rake/clean'
require './rake/gen_autoload'

TARGETS = [
    gen_autoload('src')
]

multitask default: TARGETS

desc 'Install dependencies'
task :deps do
  # todo
end

desc 'Run tests'
task :test do
  # todo
end
